<%= tag.div class: browser.device.mobile? ? "friend-requests-page-mobile" : "friend-requests-page" do %>
  <%= render "shared/sidebar" unless browser.device.mobile? %>
  <%= render "content" %>
<% end %>