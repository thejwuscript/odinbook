<div class="notification-dropdown-container" data-controller="menu" data-menu-target="notificationMenu">
  <div class="notification-dropdown-header">
    <h3>Notifications</h3>
    <span>Clear All</span>
  </div>
  <div class="notifications-list">
  <% notifications.each do |notification| %>
    <div class="notifications-list-item">
      <%= image_tag notification.sender.profile.avatar, width: 45, height: 45, class: "profile-pic-icon" %>
      <p><%= notification.message %></p>
      <span class="mdi mdi-close"></span>
    </div>
  <% end %>
  </div>
</div>
