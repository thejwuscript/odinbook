<%= render "posts/new_post_placeholder" %>

<%= turbo_frame_tag "posts" do %>
  <% @timeline_posts.each do |post| %>
    <%= render partial: "post", locals: { post: post, likes: post.likes.includes(:user) }%>
  <% end %>
<% end %>
